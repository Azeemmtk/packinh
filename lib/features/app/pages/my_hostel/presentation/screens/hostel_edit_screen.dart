import 'package:flutter/material.dart';

class HostelEditScreen extends StatelessWidget {
  const HostelEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
